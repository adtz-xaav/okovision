# Compiled source #
###################
*.com
*.class
*.dll
*.exe
*.o
*.so
 
# Packages #
############
# it's better to unpack these files and commit the raw source
# git has its own built in compression methods
*.7z
*.dmg
*.gz
*.iso
*.jar
*.rar
*.tar
*.zip
 
# Logs and databases #
######################
*.log
*.sqlite
 
# OS generated files #
######################
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Node/TS tooling (Okovision 2.0 rewrite)
node_modules/
dist/
build/
.env
.env.*
!.env.example
coverage/

# Claude Code local-only files
.claude/settings.local.json
.claude/*.local.json
.claude/scheduled_tasks.lock

# Claude Code project docs/config kept locally for development, not distributed publicly
# (agent definitions reference real operational details — NAS hostname, network layout, etc.)
CLAUDE.md
.claude/agents/

# Original PHP app, kept locally for reference during the rewrite — unlicensed upstream
# code (https://github.com/stawen/okovision), not redistributed. See issue #11.
legacy/

